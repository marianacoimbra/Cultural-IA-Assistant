[project]
name = "app"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "httpx>=0.24.0",
    "pydantic>=2.0.0",
    "python-dotenv>=1.0.0",
]
entrypoint = "main:app"

[tool.uv.workspace]
members = [
    "awesome-project",
]
